---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/buildroot
    tag: git

inputs:
  - name: cf-cli

outputs:
  - name: build

run:
  path: source/ci/tasks/assemble.sh
  args: [
    --input-dir, cf-cli,
    --output-dir, build
  ]
